import type { Block } from 'payload'

export const PhoneInlineBlock: Block = {
  slug: 'phoneInline',
  interfaceName: 'PhoneInlineBlock',
  labels: {
    singular: 'Phone',
    plural: 'Phone',
  },
  fields: [],
}
